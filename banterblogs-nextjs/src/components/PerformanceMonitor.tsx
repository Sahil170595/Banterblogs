'use client';

// PerformanceMonitor, TouchOptimizedButton, and TouchGesture removed —
// PerformanceMonitor ran continuous rAF + setInterval loops measuring
// metrics already available in browser DevTools. The touch utilities
// were unused wrappers around standard browser APIs.

'use client';

// ReadingAnalytics components removed — they displayed fake engagement data
// (random heatmaps, arbitrary engagement scores counting mousemoves).
// Keeping this file as an empty export to avoid breaking any stale imports.
